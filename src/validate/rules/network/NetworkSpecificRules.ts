import { ValidationResult, ValidationRule } from "..";

const baseName = 'NetworkSpecificRules';

export const NetworkSpecificRules: ValidationRule[] = [
    // maybe in future to handle some quirks not catught by regexes?
]

